{
  "name": "entidb-web-example",
  "version": "2.0.0-alpha.1",
  "description": "EntiDB Web Example",
  "type": "module",
  "scripts": {
    "build:wasm": "cd ../../entidb_wasm && wasm-pack build --target web --out-dir ../examples/basic/pkg",
    "dev": "vite",
    "build": "bun run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "license": "MIT"
}
