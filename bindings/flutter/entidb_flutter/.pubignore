# .pubignore for entidb_flutter
# This file specifies what should be excluded when publishing to pub.dev
# Unlike .gitignore, we WANT to include the native binaries

# Standard excludes
.dart_tool/
.packages
build/
*.iml

# Test files (not needed in published package)
test/

# Development files
.idea/
*.log

# Override root .gitignore exclusions - INCLUDE native libraries
!android/src/main/jniLibs/**/*.so
!windows/libs/*.dll
!linux/libs/*.so
!macos/Libraries/*.dylib
!ios/Frameworks/**

# Exclude placeholder READMEs from native lib directories (they're just build instructions)
android/src/main/jniLibs/README.md
windows/libs/README.md
linux/libs/README.md
macos/Libraries/README.md
ios/Frameworks/README.md
